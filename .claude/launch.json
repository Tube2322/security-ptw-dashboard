{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "soc-prototype",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server; os.chdir('Prototype integration complete'); http.server.test(HandlerClass=http.server.SimpleHTTPRequestHandler, port=int(os.environ.get('PORT','4173')))"],
      "autoPort": true
    }
  ]
}
